#!/bin/bash

# _dockerDir=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

# exec TAGS=$1 sudo docker compose \
#   -f "$_dockerDir/compose.prod.yml" \
#   "$@"

# sudo docker compose -f compose.prod.yml up -d
whoami
pwd
# sudo pwd
